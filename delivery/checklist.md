# Delivery Checklist

- Runbook for demo startup
- Sample request/response JSON
- Backtest screenshots (demo only)
- Boundary statement: no source delivery for real strategy
- Repro manifest (`repro_manifest.json`)
- Evaluation report (`evaluation_report.md`)
- Public model card (`model_card_public.md`)
- Monte Carlo + stress + intrabar metrics (`evaluation_metrics.json`)
- Runbook (`RUNBOOK.md`)
- External wording template (`external_message_template.md`)
