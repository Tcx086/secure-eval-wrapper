# Local PostgreSQL example values only.
# Copy this file to .env and replace the password with a local development value.
POSTGRES_HOST=127.0.0.1
POSTGRES_PORT=5432
POSTGRES_DB=secure_eval_wrapper
POSTGRES_USER=secure_eval_wrapper
POSTGRES_PASSWORD=example_local_password_change_me
POSTGRES_SSLMODE=disable

# Existing demo placeholders. Do not commit real credentials.
API_KEY=
API_SECRET=
RSS_BASE_URL=https://news-monitor.tcx086.com/
